#include "boat.h"

void drawBoat(float mid){
    // float vertices[] = {
        // vec3f = { mid, mid, 0 },
        // vec3f((mid - 0.05), mid, 0),
        // vec3f((mid - 0.05), (mid - 0.05),0),
        // vec3f((mid + 0.05), (mid + 0.05),0),
        // vec3f((mid + 0.05), mid,0)
    // };

    

}